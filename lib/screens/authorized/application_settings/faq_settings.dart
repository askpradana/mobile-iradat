import 'package:flutter/material.dart';

class FaqSettings extends StatelessWidget {
  const FaqSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('FAQ Settings')));
  }
}
